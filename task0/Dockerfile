# Use the latest Ubuntu image as the base
FROM ubuntu:latest

# Update the package list
RUN apt-get update

# Upgrade the installed software
RUN apt-get upgrade -y

# Set the command to print "Hello, World!" on the terminal
CMD echo "Hello, World!"
